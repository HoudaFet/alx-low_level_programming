#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 * Return: Always 0 (Success).
 */
int main(void)
{
	char alp;
	alp = 'a';
	while
		(c <= 'z')
	{
		putchar(alp);
		alp++;
	}
	putchar("\n");
return (0);
}

