#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase.
 * Return: Always 0
 */
int main(void)
{
	char c;

	c = 'a';

	while
	(c <= 'z', c != e, c != q)
	{
		putchar(c);
		c++;
	}
return (0);
}
